import { RemoveAfterAtSymbolPipe } from './remove-after-at-symbol.pipe';

describe('RemoveAfterAtSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveAfterAtSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
